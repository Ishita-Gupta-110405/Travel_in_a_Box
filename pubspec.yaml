name: travel_in_a_box
description: A new Flutter project.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  firebase_core: ^3.1.1
  firebase_auth: ^5.1.1
  cloud_firestore: ^5.0.2
  provider: ^6.1.2
  screenshot: ^3.0.0
  share_plus: ^10.1.0
  # ADD THESE TWO LINES BELOW:
  http: ^1.2.1
  google_fonts: ^6.2.1
  animated_text_kit: ^4.2.2
  carousel_slider: ^5.0.0
  flip_card: ^0.7.0
  image_picker: ^1.0.7
  google_generative_ai: 0.4.7

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/logo.png
    - assets/plane.png
    - assets/closet.jpg
    - assets/door_texture.jpg